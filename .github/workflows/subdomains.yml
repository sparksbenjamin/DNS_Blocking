name: Subdomain Enumeration per Domain File (Parallel)

on:
  workflow_run:
    workflows: ["Generate Service Blocklists"]
    types: [completed]
  workflow_dispatch:

jobs:
  prepare:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    name: Prepare domain file list
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - id: set-matrix
        run: |
          files=(services/*.txt)
          total=${#files[@]}
          batch_size=25
          batches=$(( (total + batch_size - 1) / batch_size ))

          if [ $total -eq 0 ]; then
            echo "No service files found — skipping."
            echo "matrix=[]" >> $GITHUB_OUTPUT
            exit 0
          fi

          echo "Found $total domain files, batching into $batches jobs..."
          matrix_json="["
          for ((i=0; i<$batches; i++)); do
            start=$((i * batch_size))
            batch=( "${files[@]:start:batch_size}" )
            json_batch=$(printf '%s\n' "${batch[@]}" | jq -R . | jq -s .)
            matrix_json+=$(jq -nc --argjson batch "$json_batch" '{batch: $batch}')
            if [ $i -lt $((batches - 1)) ]; then
              matrix_json+=","
            fi
          done
          matrix_json+="]"
          echo "Matrix JSON: $matrix_json"
          echo "matrix=$matrix_json" >> $GITHUB_OUTPUT

  scan:
    name: Run Subfinder on Batch
    runs-on: ubuntu-latest
    needs: prepare
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.prepare.outputs.matrix) }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.21'

      - name: Install subfinder
        run: |
          go install -v github.com/projectdiscovery/subfinder/v2/cmd/subfinder@latest
          echo "$HOME/go/bin" >> $GITHUB_PATH

      - name: Run subfinder on this batch
        run: |
          for file in ${{ join(matrix.batch, ' ') }}; do
            echo "🔍 Processing $file ..."
            > temp_subs.txt

            while IFS= read -r domain; do
              [ -z "$domain" ] && continue
              subfinder -d "$domain" -silent >> temp_subs.txt || echo "Subfinder failed for $domain" >&2
            done < "$file"

            sort -u temp_subs.txt "$file" >_
